import 'package:flutter/material.dart';

class MuyuImage extends StatelessWidget {
  const MuyuImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('assets/images/muyu.png', height: 200));
  }
}
